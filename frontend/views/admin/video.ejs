<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kelola Video - Admin</title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body>
    <div class="admin-container">
        <aside class="sidebar">
            <div class="sidebar-header"><h3><i class="fas fa-university"></i> Admin Panel</h3></div>
            <nav class="sidebar-nav">
                <a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                <a href="/admin/queues"><i class="fas fa-users"></i> Kelola Antrian</a>
                <a href="/admin/videos" class="active"><i class="fas fa-video"></i> Kelola Video</a>
                <a href="/admin/currency"><i class="fas fa-dollar-sign"></i> Kelola Kurs</a>
                <a href="/admin/settings"><i class="fas fa-cog"></i> Pengaturan</a>
                <a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </nav>
        </aside>

        <main class="main-content">
            <header class="main-header">
                <h1>Kelola Video Display</h1>
                <p>Tambah, hapus, dan atur video yang akan ditampilkan.</p>
            </header>
            
            <div class="content-body-grid">
                <div class="upload-panel">
                    <h3><i class="fas fa-upload"></i> Unggah Video Baru</h3>
                    <form id="upload-video-form">
                        <div id="drop-zone" class="drop-zone">
                            <i class="fas fa-cloud-upload-alt fa-3x"></i>
                            <p>Seret & lepas file video di sini, atau <strong>klik untuk memilih file</strong>.</p>
                            <span id="file-name-display"></span>
                        </div>
                        <input type="file" id="videoFile" name="videoFile" accept="video/*" class="file-input" required>
                        <div class="form-group">
                            <label for="display_order">Urutan Tampil</label>
                            <input type="number" id="display_order" name="display_order" placeholder="Angka (e.g., 1, 2, 3...)">
                        </div>
                        <button type="submit" class="btn btn-primary" id="upload-btn">
                            <i class="fas fa-paper-plane"></i> Unggah Video
                        </button>
                    </form>
                </div>

                <div class="video-list-panel">
                    <h3><i class="fas fa-film"></i> Daftar Video Terunggah</h3>
                    <div id="video-list" class="video-grid">
                        <p>Memuat daftar video...</p>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <div id="toast-notification" class="toast"></div>

    <script>
        window.API_BASE_URL = "<%= apiBaseUrl %>";
    </script>
    
    <script src="/js/admin.js"></script>
    <script src="/js/video.js"></script>
</body>
</html>