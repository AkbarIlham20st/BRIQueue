<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kelola Video - Admin</title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body>
    <div class="admin-container">
        <aside class="sidebar">
            <div class="sidebar-header"><h3><i class="fas fa-university"></i> Admin Panel</h3></div>
            <nav class="sidebar-nav">
                <a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                <a href="/admin/queues"><i class="fas fa-users"></i> Kelola Antrian</a>
                <a href="/admin/videos" class="active"><i class="fas fa-video"></i> Kelola Video</a>
                <a href="/admin/settings"><i class="fas fa-cog"></i> Pengaturan</a>
                <a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </nav>
        </aside>

        <main class="main-content">
            <header class="main-header">
                <h1>Kelola Video Display</h1>
                <p>Tambah, hapus, dan atur video yang akan ditampilkan di layar publik.</p>
            </header>
            
            <div class="content-body">
                <section class="card">
                    <h3><i class="fas fa-upload"></i> Unggah Video Baru</h3>
                    <form id="upload-video-form" enctype="multipart/form-data">
                        <div class="form-group">
                            <label for="videoFile">Pilih File Video (Max 50MB)</label>
                            <input type="file" id="videoFile" name="videoFile" accept="video/*" required>
                        </div>
                        <div class="form-group">
                            <label for="display_order">Urutan Tampil (Opsional)</label>
                            <input type="number" id="display_order" name="display_order" placeholder="Contoh: 1">
                        </div>
                        <button type="submit" class="btn btn-primary">Unggah Video</button>
                        <p id="upload-status" class="upload-status"></p>
                    </form>
                </section>

                <section class="video-list-container">
                    <h3><i class="fas fa-film"></i> Daftar Video</h3>
                    <div id="video-list" class="video-grid">
                        <p>Memuat daftar video...</p>
                    </div>
                </section>
            </div>
        </main>
    </div>
    <script src="/js/admin.js"></script>
    <script src="/js/video.js"></script>
</body>
</html>